# Data Policy

The tutorial defaults to tiny synthetic graphs and small built-in examples so a
learner can run it without external credentials, private datasets, or a GPU.

Large datasets should not be committed. If a future chapter uses a public dataset,
document the source, expected size, cache location, and fallback synthetic example.
